# Copy to .env and fill in. NEVER commit the real .env.
META_PIXEL_ID=your_pixel_or_dataset_id
META_CAPI_TOKEN=your_conversions_api_access_token

# Optional: while testing, set this to the code from Events Manager > Test Events.
# Remove it for production so events count as live.
# META_TEST_CODE=TEST12345

# Comma-separated origins allowed to POST here.
ALLOWED_ORIGINS=https://servicespartan432.github.io,https://affordableairdirect.com

PORT=8788
GRAPH_VERSION=v21.0
